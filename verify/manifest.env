# Uber MVP — e2e verify manifest (consumed by ~/Hermes/bin/e2e-verify)
MODE=compose
UP=docker compose up --build -d
READY=curl -f http://localhost:${APP_PORT:-8020}/healthz
TEST_DEPS=httpx
ACCEPTANCE=pytest verify/acceptance/ -v
DOWN=docker compose down -v
