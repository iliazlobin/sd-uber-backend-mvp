"""Verification suite package (acceptance tests).

Present so `from verify.acceptance...` resolves on a clean checkout without a
PYTHONPATH override (pytest's prepend importmode walks up to the repo root).
"""
